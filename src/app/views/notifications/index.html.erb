<%= render partial: 'notification', collection: @notifications %>

<%= link_to 'Load More', notifcations_path(current_user, offset: @next), remote: true, class: 'button is-fullwidth more' if @next %>

