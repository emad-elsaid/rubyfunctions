var el = document.querySelector('a.more');
var newEl = document.createElement('div');
newEl.innerHTML = '<%= escape_javascript(render 'notifications', notifications: @notifications) %>';
el.parentNode.replaceChild(newEl, el);
