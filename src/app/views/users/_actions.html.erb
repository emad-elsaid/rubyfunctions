<div class="card m-b-md is-shadowless">
  <div class="card-content">
    <% if current_user && user != current_user %> 
        <%= link_to_if follows?(user), 'Unfollow', user_followerships_path(user), class: 'button is-pulled-right', method: :delete do  %>
           <%= link_to 'Follow', user_followerships_path(user), class: 'button is-pulled-right', method: :post  %>
        <% end %>

        <div class="is-clearfix"></div>
    <% end %>
  </div>
</div>